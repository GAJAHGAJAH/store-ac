import 'package:flutter/material.dart';

class AppColors {
  // Cool AC & Fresh Breeze Theme (Cool Blue/Cyan & Ice Blue)
  static const Color primary = Color(0xFF0091EA);      // Cool Blue / Deep Sky Blue
  static const Color primaryLight = Color(0xFFE1F5FE); // Light Ice Blue
  static const Color primaryDark = Color(0xFF01579B);  // Deep Cool Blue
  static const Color accent = Color(0xFF00B8D4);       // Vibrant Cyan / Soft Breeze

  static const Color background = Color(0xFFF4F9FC);   // Off-White Ice Blue
  static const Color surface = Color(0xFFFFFFFF);      // White
  static const Color error = Color(0xFFD32F2F);        // Red
  static const Color info = Color(0xFF0288D1);         // Light Blue

  static const Color textPrimary = Color(0xFF263238);  // Dark Slate Gray Text
  static const Color textSecondary = Color(0xFF455A64);// Slate Gray Text
  static const Color textHint = Color(0xFF90A4AE);     // Light Slate Hint
  static const Color textOnPrimary = Color(0xFFFFFFFF);// White text on primary

  static const Color divider = Color(0xFFE0F2F1);      // Soft Ice Divider
  static const Color border = Color(0xFFB2EBF2);       // Soft Cyan Border
}